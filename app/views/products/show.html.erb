<%= stylesheet_link_tag "colorbox" %>
<%= javascript_include_tag "jquery.colorbox-min" %>
<script type="text/javascript">
  $(document).ready(function() {
    jQuery('a.gallery').colorbox({ 
      closeButton: true,
      opacity: 1.9,
      });
  });
</script>
<p id="notice"><%= notice %></p>

<p>
  <strong>Product number:</strong>
  <%= @product.product_number %>
</p>

<p>
  <strong>Name:</strong>
  <%= @product.name %>
</p>

<p>
  <strong>Description:</strong>
  <%= @product.description %>
</p>

<p>
<table border=1>
  <strong style ="float: left;">Image:</strong>
  <% @product.images.each do |image| %>
    <tr><td><%= link_to image_tag(image.avatar.url(:thumb)), image.avatar.url, html_options = {:class => "gallery", :title => "Detail"  } %></td>
    <td><%= link_to 'Destroy', "/images/#{image.id}", :confirm => 'Are you sure?', :method => :delete %></td></tr>
  <% end %>
</p>
</table>
<br/><br/><br/><br/><br/><br/>
<%= link_to 'Edit', edit_product_path(@product) %> |
<%= link_to 'Back', products_path %>
